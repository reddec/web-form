<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{{or .Form.Title .Form.Name .Form.Table}}</title>
    <link rel="stylesheet" href="../static/css/bulma.min.css">
</head>
<body>

<div class="container">
    <section class="section">
        <h1 class="title"> {{or .Form.Title .Form.Name .Form.Table}}</h1>
        <p>{{($.Render .Form.Description) | markdown}}</p>
    </section>
    <br/>
    <div class="box">
        <form method="post">
            <input type="hidden" name="_xsrf" value="{{.XSRF}}"/>
            <input type="hidden" name="__fresh" value="true"/>
            <div class="field">
                <label class="label">
                    Form access code
                </label>
                <div class="control">
                    <input class="input" type="password" name="__access_code" placeholder="enter access code..."
                           required/>
                </div>
            </div>
            <div class="field">
                <div class="control">
                    <button class="button is-success" type="submit">open form</button>
                </div>
            </div>
        </form>
        {{- range .Flashes}}
            <br/>
            <div class="notification is-{{.Type}}">
                {{.Text}}
            </div>
        {{- end}}
    </div>
</div>
</body>
</html>